/** Endpoint for the frontend API client. */
export const API_PORT = 3000
export const API_HOST = "localhost"
export const API_URL = `http://${API_HOST}:${API_PORT}`
export const API_WEBSOCKET = `ws://${API_HOST}:${API_PORT}/api/ws`