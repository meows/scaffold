import { RegisterForm } from "#/auth/RegisterForm"

export default function RegisterPage() {
  return (
    <main className="grid h-svh place-items-center bg-secondary">
      <RegisterForm />
    </main>
  )
}
